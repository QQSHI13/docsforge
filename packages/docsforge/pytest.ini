[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    e2e: browser E2E tests (Playwright/Chromium; skip if unavailable)
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
